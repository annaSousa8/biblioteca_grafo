# COMO RODAR?

Para rodar a biblioteca é preciso ter instalado o compilador gcc, para verificar rode:

´´´
gcc --version
´´´

Para rodar os testes, rode:

´´´
gcc -Iinclude testes/teste.c src/grafo.c -o bin/grafo
´´´
